import { type IconBaseProps } from 'react-icons';
import { HiOutlineFilm } from 'react-icons/hi2';

const IconAnimatedStyle = ({ ...props }: IconBaseProps) => {
  return <HiOutlineFilm {...props} />;
};

export default IconAnimatedStyle;
