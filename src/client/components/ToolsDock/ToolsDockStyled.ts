import { styled } from '@stitches/react';

export const ToolsDockContriner = styled('div', {
  position: 'fixed',
  bottom: '0.5rem',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  width: '100%',
  zIndex: 1,
  textTransform: 'capitalize',
});
