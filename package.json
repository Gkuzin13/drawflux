{
  "name": "sketch-app",
  "version": "0.1.0",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "packages:lint": "pnpm --filter '{./packages/**}' lint",
    "packages:fix-all-files": "pnpm --filter '{./packages/**}' fix-all-files",
    "packages:typecheck": "pnpm --filter '{./packages/**}' typecheck"
  },
  "dependencies": {
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.29.8",
    "eslint": "^8.38.0",
    "prettier": "^2.8.7",
    "typescript": "^4.9.5",
    "vite": "^4.2.1",
    "vitest": "^0.29.8"
  }
}
