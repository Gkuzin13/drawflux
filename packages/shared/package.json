{
  "name": "shared",
  "description": "Shared modules",
  "version": "1.0.2",
  "private": true,
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "eslint . --ext .ts",
    "fix-all-files": "eslint --fix . --ext .ts",
    "typecheck": "tsc --project ./tsconfig.json --noEmit"
  },
  "dependencies": {
    "@stitches/react": "*"
  },
  "devDependencies": {
    "eslint-config-bases": "workspace:^"
  }
}
